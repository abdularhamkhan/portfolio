"use client";

/**
 * Renders nothing. Kept so a stale dev-server graph that still resolves this
 * path cannot break compilation (missing file → broken /_next/static assets).
 */
export function ScrollProgress() {
  return null;
}
